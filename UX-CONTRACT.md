# UX Contract

Core journey: Create Request → Edit → Save → Plan → Generate → Test → Repair → Validate → Go-Live → Monitor → Audit.

Every operation must expose pending, success, error, retry/recovery, and audit feedback.